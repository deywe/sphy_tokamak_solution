
# SPHY Engine: Visual Validator & Reality Auditor

This repository contains the **SPHY Visual Validator**, a high-performance engine designed to audit and visualize the transition from classical entropy (Hilbert Space) to absolute synchrony (SPHY Geodesics).

By using **Parquet** datasets and **SHA-256** validation, this tool proves that stability in complex systems—like quantum matrices or nuclear fusion reactors—is achieved through phase synchronization, not brute force.

## 🚀 The Visualizer: `visualize_and_validate_sphy_en1.py`

The visualizer acts as a witness to the data. It does not "simulate" randomness; it reads a pre-calculated geodetic sequence where every frame has been mathematically sealed.

### Key Features:
* **Parquet Integration:** Direct ingestion of SPHY sequence files (up to 12,000+ frames).
* **SHA-256 Integrity:** Displays the unique hash for every frame, cross-referencing the dataset's internal audit trail.
* **Paradigm Transition:** Real-time visualization of the migration from **Hilbert (Red/Chaos)** to **SPHY (Cyan/Order)**.
* **3D Interactive Audit:**
    * **Mouse Drag:** Rotate the 3D Geodesic field.
    * **Mouse Wheel:** Zoom into the singularity.
    * **Resizable Window:** Adaptation to any workspace.

## 📊 Dataset: `sphy_sequence_12000frames.parquet`

The engine is powered by a high-fidelity dataset. 
* **12,000 Frames:** A complete cycle of reality stabilization.
* **Deterministic Metadata:** Each row contains `mix` (paradigm shift), `jitter` (entropy level), `estabilidade` (coherence %), and the `sha256` signature.

## 🛠️ How to Run

1.  **Install Dependencies:**
    ```bash
    pip install py5 pandas pyarrow numpy
    ```
2.  **Execute the Auditor:**
    ```bash
    python visualize_and_validate_sphy_en1.py
    ```
3.  **Load the Truth:** When prompted, enter `sphy_sequence_12000frames.parquet`.

## 🧠 The SPHY Philosophy

Traditional physics attempts to control systems through "Isolation" and "Brute Force" (The Hilbert Ego). **SPHY (Symbiotic Phase Harmonic Yielding)** proves that by opening the system and tuning it to the local space-time resonance ($\Phi$), we reach **Absolute Sovereignty** over entropy.

> "Don't believe the simulation. Audit the hash. Verify the geometry."

---
**Developed by:** Deywe Okabe | *Black Swan Research - Harpia SPHY Division*
